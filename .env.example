# Copy to .env for local dev. Never commit real values.

# Required
MONGODB_URI="mongodb+srv://USER:PASS@sovereignportal.gekj125.mongodb.net/?appName=SovereignPortal"
JWT_SECRET="generate-with-openssl-rand-base64-32"

# Optional — when unset, falls back to console-log adapter (dev only)
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_VERIFY_SERVICE_SID=""

PORT="8080"
LOG_LEVEL="info"
